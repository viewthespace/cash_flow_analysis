require 'spec_helper'

describe CashFlowAnalysis do
  it 'has a version number' do
    expect(CashFlowAnalysis::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
